# Service A : la MEME image, deployee en frontal internet.
project:
  name: "checkout-api"
  description: "Public checkout API handling card payments for the storefront"

context:
  exposure: "internet"
  data_sensitivity: "critical"
  business_criticality: "critical"
  availability_requirement: "critical"

  compliance_requirements:
    - "PCI-DSS"
    - "GDPR"

  audit_requirement: "high"

  # Ce qu un service dans le perimetre PCI-DSS a reellement devant lui.
  controls:
    waf: true
    ddos_protection: true
    ids: true
    siem: true
    edr: true
    antivirus: false
    segmentation: true
    zero_trust: false

  notes: "Internet-facing REST API behind a WAF and a CDN. Terminates TLS, accepts unauthenticated traffic on the payment initiation route, parses attacker-controlled JSON, and holds cardholder data plus customer PII. Reachable from the public internet on port 443."
